
#alias dockerbuild 'docker build --build-arg http_proxy=$http_proxy --build-arg https_proxy=$https_proxy'

# To rebuild  the docker image, on vs-lin64-12, cd to this directory and type:

#      %  dockerbuild -t xed-testing-container .

