language: c

os:
    - linux
    - osx

# test gcc and clang
compiler:
    - gcc
    - clang

# 32 bit build is broken, need to install 32 bit libs
env:
#    - host_cpu=ia32
    - host_cpu=ia64

script:
    - git clone https://github.com/intelxed/mbuild.git
    - mkdir build
    - cd build && ../mfile.py host_cpu=$host_cpu test
